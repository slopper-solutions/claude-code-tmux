#!/bin/sh
# DEPRECATED: forwards to agent-spawn-sandbox --harness=claude.
# See bin/agent-spawn-sandbox (canonical) and skills/spawn-agent-sandbox/SKILL.md.
set -e
echo "claude-spawn-sandbox: DEPRECATED — use 'agent-spawn-sandbox' instead." >&2
exec agent-spawn-sandbox --harness=claude "$@"
