# Anthropic Claude Code — the reference adapter.
# Supports --rc mobile reach and --dangerously-skip-permissions.
# Schema (shared across all harness files): BINARY, RC_FLAG, RC_TAKES_NAME,
# YOLO_FLAG, SANDBOX_OK, STATUS. See CLAUDE.md for field semantics.
BINARY=claude
RC_FLAG=--rc
RC_TAKES_NAME=1
YOLO_FLAG=--dangerously-skip-permissions
SANDBOX_OK=1
STATUS=stable
