# Google Gemini CLI. No mobile-reach analog (RC_FLAG empty by design).
# Sandbox is built into gemini itself via --sandbox / -s, so SANDBOX_OK=0
# keeps agent-spawn-sandbox from double-sandboxing it.
BINARY=gemini
RC_FLAG=
RC_TAKES_NAME=0
YOLO_FLAG=--yolo
SANDBOX_OK=0
STATUS=stub
