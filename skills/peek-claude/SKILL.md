---
name: peek-claude
description: DEPRECATED — use the harness-neutral `agent-peek` skill instead. This skill is kept only so existing references resolve. New work should call `agent-peek`, not `claude-peek`.
---

# peek-claude (deprecated)

Superseded by [`agent-peek`](../agent-peek/SKILL.md). The underlying `claude-peek` script is now a thin forwarder.

Migration: replace `claude-peek ...` with `agent-peek ...` — usage is identical.
